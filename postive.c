#include <stdio.h>

int main(void) {
	int num;
	scanf("%d",&num);
	if(num>0)
	{
		printf("the number is negative");
	}
	else
	{
		printf("the number is postive");
	}else if(num==0)
	{
		printf("the number is 0");
	}
}
